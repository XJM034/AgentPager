## Design Context

### Users

主要用户是重度使用 Codex 的开发者。旧 Android 手机会长期横放在桌面上，作为可远看、可触控的专用任务状态终端。

### Brand Personality

精密、鲜活、像素化。它应像一件独立硬件，而不是缩小后的管理后台。

### Aesthetic Direction

- 深蓝近黑、横屏、任务列表优先。
- 每个任务行左侧只有一枚紧凑的 3×3 彩色像素核心，文字信息是主体。
- 像素必须是硬直角并保留明显暗色间隙。
- 每个亮格单独拥有内、中、外三层 Bloom；光晕跟随该格位移，暗格不发光。
- 同一状态只使用一个色相家族，不使用彩虹配色。
- 子代理只在父任务展开区内显示，使用更深一级表面和紧凑树状行，不拆成顶层任务。
- 不使用人物、拟人头像、渐变文字、玻璃卡片和通用 AI 仪表盘布局。
- 全部界面文字使用支持简体中文的 Fusion Pixel 12px 字体。

### Design Principles

1. 一眼同时读到任务标题、用户原话、最新步骤、Token 和耗时。
2. 默认显示紧凑任务行，点击后在原位展开；审批和提问自动展开并抢占焦点。
3. 运行中的子代理会自动展开父任务；每个子代理只显示名称、状态、耗时、最新一步和 Token，终态显示约 4 秒后清除。
4. 状态变化优先于装饰动画；审批和提问必须立即抢占焦点。
5. 背景固定为 `#05070B`，展开表面为 `#0B1018`，不使用纯黑、渐变或玻璃效果。
6. 格子强度、位置和缩放连续插值；活跃状态 60 FPS，空闲 30 FPS，终态播放一次后静止。
