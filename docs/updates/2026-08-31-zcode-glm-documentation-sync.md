# ZCode / GLM 文档同步与用户实测反馈补录

记录日期：2026-08-31。

本轮经用户批准，仅补齐 Agent 文档与阅读入口。核对基线为 `alexx_custom` 的
`64eec57351de840b53462459dd25016140a26675`；开场工作区干净。
没有修改产品代码、协议、测试、凭据、真实配置或已安装应用。

## 用户实测反馈

来源：此前本会话中用户在请求提交和推送工作区内容时反馈：

> 我刚刚测了一下 glm 额度检测是可用的

这是用户实测报告，不是本轮 Agent 发起的测试。补录日期不是测试时间。
用户未提供此次测试的具体构建、Key 配置方式、两个窗口数值、重置时间、
官网同时间对照或截图；本轮没有读取凭据、调用真实额度接口或操作手机来补齐证据。

因此，后续 Agent 应保留“用户已报告 GLM 额度检测可用”，同时做到：

- 不再把“当时未验证真实 GLM”概括为“用户从未测试过”或“功能一定不可用”。
- 不推断用户如何取得或保存 Key，也不把该反馈视为完整端到端验收。
- 不据此宣称复用 ZCode 登录态的无重复 Key 接入已经实现。
- 保留 [#10 原验收记录](2026-08-29-issue-10-final-zcode-redmi-acceptance.md)的历史范围；
  [#12 Gate](2026-08-29-issue-12-no-duplicate-key-glm-quota-gate.md)针对无重复 Key 目标，任务状态仍需另行回读。

## 文档缺口与修正

1. 已有调研/PRD 在上轮提交中标为历史基线，但文档首页缺少现行功能入口。
   新增 [ZCode / GLM 现行功能与验收边界](../zcode-glm-integration.md)，集中路由代码、协议、测试和验证记录。
2. 根 `AGENTS.md` / `CLAUDE.md` 同步增加条件式阅读入口与明确的镜像维护规则，
   Hook 配置保护范围补全 ZCode；不把功能细节堆入根文档。
3. macOS 平台说明与开发文档补全 ZCode Hook、GLM 额度职责和真实验收边界；
   Android 平台说明增加相关任务的阅读入口。
4. 将本节用户反馈持久化，并分开记录独立 Key 已有实现、用户反馈与无重复 Key 未实现目标。

本轮使用 `claude-agents-bootstrap` 和 `writing-for-agents`：以短入口引用按需文档，
保留代码事实、用户反馈和历史证据的区别。没有新建 `CONTEXT.md`、ADR、活动队列、
技能副本或子目录规则文件；没有删除或迁移历史文档，因此无需新增归档副本。
现有 Skill 来源、项目副本、加载入口和 Hook 配置均保持不变。

## 检查与未验证项

- 对照生产代码和共享协议核对 ZCode 生命周期、权限往返、GLM 数据来源与顶栏条件。
- 文档检查通过：8 个 Markdown 文件的 UTF-8/换行与敏感值扫描、73 个本地链接、
  根镜像一致性、6 个现行功能入口和精确修改范围；暂存区为空。
- `git diff --check` 通过；产品源码、协议、测试及 Agent/Entire 配置没有差异。
- 本轮未运行 Swift/Gradle 测试、构建、真实 GLM 请求、ZCode Session 或 Redmi 验收；没有产品源码变化。
- 未启动全新 Codex/Claude 任务验证文档自动加载；入口可达不等于每种 Agent 都已实测加载。
- 未更新、评论或关闭 GitHub Issue；没有提交、推送、PR、Release 或 Entire Review。

## 后续补充：大型功能文档同步规则

同日，用户要求将“大型功能提交并确认后同步文档，让其他 Agent 知晓进度”固化为规则，
并指定 `ask-matt` 核对适合的流程。本节记录后续规则修改，不扩大前述产品验证范围。

- 核对 `ask-matt`、`implement`、`domain-modeling` 与 Matt 的 `handoff` 原文后，确认这些流程分别覆盖实现、
  领域记录和便携交接，`implement` 没有通用文档同步收尾清单。
- 在 [长期开发流程](../custom-development-workflow.md)加入该清单；根 `AGENTS.md` / `CLAUDE.md`、
  文档索引与 Issue Tracker 说明添加入口。它是项目规则，不是新增 Hook、Skill 副本或 CI。
- 阶段收尾、提交后的用户确认/验收反馈、已确认需求变更和阻塞暂停均触发本地文档同步。
  现行能力、日期化证据和 Ticket 状态分层记录；保留原始目标、降级差异、未验证项与有限豁免。
- 继续使用 `claude-agents-bootstrap` 与 `writing-for-agents` 保持双入口简短镜像；
  不新建空术语表、ADR 或活动队列，也不改变提交、推送、关票和发布的授权边界。
- 本次规则修改开始时，另一个任务的 `BridgeViews.swift` 已有未提交改动，本轮不修改或纳入其内容。
- 文档检查通过：连同此前未提交文档，共 10 个 Markdown 文件、84 个本地链接；
  UTF-8/换行、敏感值模式扫描、根镜像和 `git diff --check` 均通过，暂存区为空。
  精确范围检查首次发现并行任务新增的 `2026-08-31-macos-settings-configuration-ui.md`，将其明确排除后复核通过；
  本轮未修改该文件，`BridgeViews.swift` 的 SHA-256 与本轮开场一致。
- 本次只新增文档规则，没有运行产品构建、真实验收或新 Session 加载验证；未提交、推送或操作 GitHub。
